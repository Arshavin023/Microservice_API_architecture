# server {
#     listen 80;
#     server_name localhost;
#     root /usr/share/nginx/html;
#     index index.html;

#     # Use Docker's internal DNS resolver — resolves upstream names at
#     # request time rather than startup, so nginx starts even if a backend
#     # container isn't yet running.
#     resolver 127.0.0.11 valid=10s;

#     location / {
#         try_files $uri $uri/ /index.html;
#     }

#     location /api/auth/ {
#         set $auth http://auth-service:8000;
#         proxy_pass $auth/auth/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }

#     location /api/users/ {
#         set $users http://user-service:8000;
#         proxy_pass $users/users/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }

#     location /api/products/ {
#         set $products http://product-service:8000;
#         proxy_pass $products/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }

#     location /api/orders/ {
#         set $orders http://order-service:8000;
#         proxy_pass $orders/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }

#     location /api/payments/ {
#         set $payments http://payment-service:8000;
#         proxy_pass $payments/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }

#     location /api/shipping/ {
#         set $shipping http://shipping-service:8000;
#         proxy_pass $shipping/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
# }

server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    resolver 127.0.0.11 valid=10s;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/auth/ {
        set $upstream http://auth-service:8000;
        rewrite ^/api/auth/(.*)$ /auth/$1 break;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/users/ {
        set $upstream http://user-service:8000;
        rewrite ^/api/users/(.*)$ /users/$1 break;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/products/ {
        set $upstream http://product-service:8000;
        rewrite ^/api/products/(.*)$ /$1 break;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/orders/ {
        set $upstream http://order-service:8000;
        rewrite ^/api/orders/(.*)$ /$1 break;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/payments/ {
        set $upstream http://payment-service:8000;
        rewrite ^/api/payments/(.*)$ /$1 break;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/shipping/ {
        set $upstream http://shipping-service:8000;
        rewrite ^/api/shipping/(.*)$ /$1 break;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}